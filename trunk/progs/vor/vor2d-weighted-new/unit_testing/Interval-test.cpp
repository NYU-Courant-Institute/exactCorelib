#include <assert.h>
#include "Interval.h"

const Interval p(1, 2);
const Interval n(-5, -3);
const Interval m(-7, 11);

int main(int argc, char** argv) {
  

  return 0;
}

bool test_un_minus() {
  Interval 
  Interval i = -
}

